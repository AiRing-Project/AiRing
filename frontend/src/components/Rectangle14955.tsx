import * as React from 'react';
import {StyleSheet, View} from 'react-native';

const Rectangle14955 = (props: any) => {
  return <View style={[styles.rectangleView, props.style]} />;
};

const styles = StyleSheet.create({
  rectangleView: {
    backgroundColor: 'rgba(234, 234, 234, 0.3)',
    width: '100%',
    height: 11,
  },
});

export default Rectangle14955;
